CREATE TABLE stations (
                          id UUID,
                          post_code VARCHAR(5) NOT NULL,
                          latitude DECIMAL(13,11) NOT NULL,
                          longitude DECIMAL(13,11) NOT NULL
);